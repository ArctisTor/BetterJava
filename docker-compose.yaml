version: "3.8"
services:
  #betterjava:
    #build: . # /path/to/dockerfile or https://github.com/my-company/my-project.git
    #image: betterjava-image #name of the image
    #ports:
     # - "8080:9000"
    #depends_on:
     # - postgres_db
    #environment:
     # POSTGRES_PASSWORD : admin
     # POSTGRES_USER : admin
  postgres_db:
    image: 'postgres:17'
    restart: always
    container_name: postgres_db
    ports:
      - "9001:5432"
    environment:
      POSTGRES_USER : admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: VTubers
      PGDATA: /var/lib/postgresql@17/data/VTubers
    volumes:
    - ./data:/var/lib/mysql
    - ./VTubers.sql:/docker-entrypoint-initdb.d/VTubers.sql
